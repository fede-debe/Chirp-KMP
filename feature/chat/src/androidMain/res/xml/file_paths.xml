<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Camera capture files live in the app cache root (File.createTempFile(..., cacheDir)). -->
    <cache-path
        name="camera_captures"
        path="." />
</paths>
